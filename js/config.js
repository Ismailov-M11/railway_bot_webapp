const CONFIG = {
    // URL where the bot's API server is running (port 8080 by default).
    // Set this to your bot's public URL after deployment.
    // Example: 'https://your-bot.railway.app'
    API_BASE_URL: '',
    MAX_ROUTES: 5,
};
